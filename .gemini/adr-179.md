# Adopt Eloquent ORM Models as Standard Data Access Layer: Controllers Services Not

## Policies

1. Controllers and services MUST NOT bypass models by executing raw SQL queries directly for standard CRUD operations
