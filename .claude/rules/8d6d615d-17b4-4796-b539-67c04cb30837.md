<rule_activation id="8d6d615d-17b4-4796-b539-67c04cb30837" title="Adopt Eloquent ORM Models as Standard Data Access Layer: Each Model Correspond" applies_to="**/*">
These rules are ALWAYS ACTIVE for all database-backed entities in the application. All data access operations MUST use Eloquent ORM models as the primary abstraction layer.
</rule_activation>

### Rules

- **R-ELOQUENT-001** MUST: Each model MUST correspond to a single database table with the table name following Laravel's naming conventions (plural, snake_case).
- **R-ELOQUENT-002** MUST: All model files MUST exist in app/Models namespace and extend Illuminate\Database\Eloquent\Model.
- **R-ELOQUENT-003** MUST: Each model MUST define either $fillable or $guarded properties immediately after model creation to prevent mass assignment vulnerabilities.
- **R-ELOQUENT-004** SHOULD: Document relationship methods with PHPDoc annotations specifying return types for better IDE support and static analysis.
- **R-ELOQUENT-005** SHOULD: Use model events (creating, created, updating, updated) for cross-cutting concerns like logging and auditing rather than duplicating logic in controllers.
- **R-ELOQUENT-006** SHOULD: Leverage Laravel's attribute casting ($casts property) to automatically convert database values to appropriate PHP types (dates, JSON, booleans).
- **R-ELOQUENT-007** SHOULD: Use model factories for testing to generate realistic test data without manual array construction.
- **R-ELOQUENT-008** MUST: No direct DB facade calls (DB::select, DB::insert, DB::update, DB::delete) for CRUD operations in controllers or services without exception documentation.

### Verify

```bash
# Count models extending Eloquent Model
grep -r 'extends Model' app/Models/ | wc -l

# Find models not in correct namespace
find app/Models -type f -name '*.php' | xargs grep -L 'namespace App\\Models'

# Display model information
php artisan model:show --all | grep -E '(Table|Database)'

# Detect direct DB facade calls in application layer
grep -r 'DB::select\|DB::insert\|DB::update\|DB::delete' app/Http/Controllers/ app/Services/ | grep -v 'raw query approved'
```

**Accept when:**
- All model files exist in app/Models namespace and extend Illuminate\Database\Eloquent\Model
- No direct DB facade calls for CRUD operations found in controllers or services (excluding approved exceptions)
- Model count matches database table count for domain entities (excluding pivot tables and migrations table)
- All models define either $fillable or $guarded properties for mass assignment protection
- Relationship methods include PHPDoc annotations with return types
- No mass assignment vulnerabilities detected in model definitions

<enforcement>
Claude Code MUST NOT skip or defer verification. All Eloquent ORM model implementations MUST pass the verification commands before acceptance. Violations detected by CI pipeline checks using PHPStan or Psalm MUST be remediated. Pull requests MUST be blocked until models are created for new database tables. Code review checklist MUST verify model existence for new database tables.
</enforcement>