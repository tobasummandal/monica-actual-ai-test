<rule_activation id="4698a97f-45d8-4a02-8b10-c11b726766a9" title="Adopt Eloquent ORM Models as Standard Data Modeling Pattern: Models Define Custom" applies_to="**/*">
These rules are ALWAYS ACTIVE for all data modeling activities within the application layer.
</rule_activation>

### Rules

- **R-29-008** MUST: Enforce single responsibility principle; extract business logic to service classes rather than bloating models with multiple responsibilities.
- **MAY** Models MAY define custom attributes using accessors and mutators for computed or transformed data.

### Verify

```bash
# Verify all model files extend Eloquent Model
find app/Models -type f -name '*.php' | xargs grep -L 'extends Model' | wc -l | grep -q '^0$'

# Verify proper namespace usage
grep -r 'namespace App\\Models' app/Models/*.php | wc -l

# Verify model class structure
php artisan tinker --execute="echo count(get_declared_classes())" 2>&1 | grep -q '[0-9]'
```

**Accept when:**
- All PHP files in app/Models directory extend Illuminate\Database\Eloquent\Model class
- Model classes are properly namespaced under App\Models and follow PSR-4 autoloading standards
- No direct database queries using DB facade exist in controller methods where Eloquent models are available
- Code review checklist includes verification of mass assignment protection and relationship eager loading
- Models define $fillable or $guarded arrays for mass assignment protection
- Relationships are defined in dedicated methods using Eloquent relationship types
- Model observers or events are used for cross-cutting concerns rather than duplicating logic in controllers

<enforcement>
Claude Code MUST verify all conditions in the Accept when section before approving data modeling patterns. Automated static analysis using PHPStan or Psalm MUST verify model class structure. Code review MUST block merges if models contain business logic beyond data access concerns or lack mass assignment protection. Violations in CI pipeline MUST fail if models exist outside app/Models namespace or do not extend base Model class.
</enforcement>