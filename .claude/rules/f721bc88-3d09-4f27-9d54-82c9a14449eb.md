<rule_activation id="f721bc88-3d09-4f27-9d54-82c9a14449eb" title="Standardize Password Hashing for Authentication Operations: Plain Text Passwords" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files handling authentication operations, including account creation, password resets, password updates, invitation acceptance, and all Fortify action handlers.
</rule_activation>

### Rules

- **R-PWD-001** MUST_NOT: Plain-text passwords MUST NOT be stored in the database or logged in any application logs.
- **R-PWD-002** MUST: Use `Hash::make($password)` for all password hashing operations; configure cost factor in `config/hashing.php`.
- **R-PWD-003** MUST: In Fortify action classes (`ResetUserPassword`, `UpdateUserPassword`), ensure password hashing occurs before calling `$user->forceFill(['password' => ...])->save()`.
- **R-PWD-004** MUST: In domain service classes (`CreateAccount`, `AcceptInvitation`), apply hashing before user model creation or password updates.
- **R-PWD-005** MUST: Feature tests should use `Hash::check($plainPassword, $user->password)` to verify hashing occurred correctly.
- **R-PWD-006** SHOULD: Consider using Laravel's validation rule `Password::min(8)->mixedCase()->numbers()` to enforce password complexity before hashing.

### Scope

**In scope:**
- Account creation workflows (new user registration)
- Password reset operations (forgotten password flows)
- Password update operations (authenticated user password changes)
- Invitation acceptance flows that set initial passwords
- All Fortify action handlers dealing with password operations
- Domain service classes that create or modify user credentials

**Out of scope:**
- API token generation and management
- OAuth or third-party authentication flows that don't store passwords
- Session management and cookie handling
- Two-factor authentication token handling
- Password reset token generation (tokens themselves, not the new password)

### Verify

```bash
# Check for unhashed password assignments
grep -r "'password'\s*=>\s*\$" app/ --include='*.php' | grep -v 'Hash::make' | grep -v 'bcrypt(' | grep -v '// ' || echo 'No unhashed password assignments found'

# Count hashed password operations in authentication code
grep -r "Hash::make\|bcrypt(" app/Actions/Fortify/ app/Domains/Settings/ --include='*.php' | wc -l

# Verify password tests
php artisan test --filter=Password --coverage-text | grep -E '(UpdatePasswordTest|ResetPasswordTest)'
```

**Accept when:**
- All password assignment operations in authentication code paths use `Hash::make()` or `bcrypt()` helper
- Feature tests verify that stored passwords are hashed (`Hash::check` returns true for original password)
- Code review checklist includes verification of password hashing for any authentication-related changes
- No grep patterns detect potential unhashed password assignments in production code

<enforcement>
Claude Code MUST NOT skip or defer verification. Password hashing is a non-negotiable security requirement with no exceptions permitted for production authentication code. CI pipeline MUST fail if unhashed password assignments are detected. Code review MUST block merge if password hashing is not verified in authentication code.
</enforcement>

### Implementation Context

This ADR addresses a critical security concern where password hashing must be applied uniformly across all authentication entry points. The pattern signature (94967224950efe6ab90490e925ef2951) appears consistently across 5 authentication-related files with 81.88% confidence, indicating this is a deliberate architectural pattern that must be maintained.

**Key risks to monitor:**
- Developers forgetting to hash passwords in new authentication code paths
- Legacy code or database migrations containing unhashed passwords
- Insufficient bcrypt cost factor becoming inadequate as computing power increases

**Mitigation approaches:**
- Implement automated tests verifying password hashing in all authentication workflows
- Add code review checklist items for authentication changes
- Consider database constraints or model observers to detect unhashed passwords
- Periodically review and update bcrypt cost factor in configuration