<rule_activation id="9e0f9479-7763-43f5-8ae3-8f2891465f42" title="Organize Test Files by Domain and Feature Modules: Domain Boundaries Enforced" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching `**/*`.

Domain-driven test organization enables selective test execution, improves discoverability, and maintains clear relationships between production code and test files.
</rule_activation>

### Rules

- **R-TDD-001** SHOULD: Domain boundaries SHOULD be enforced in CI/CD pipelines to enable selective test execution by domain or feature module
- **R-TDD-002** SHOULD: Test files SHOULD be organized under tests/Unit/Domains/ mirroring the production code structure
- **R-TDD-003** SHOULD: Test file names SHOULD consistently append 'Test' suffix to production class names
- **R-TDD-004** SHOULD: Domain feature modules (e.g., ManageTemplates, ManageUserPreferences, ManageUsers) SHOULD be represented as subdirectories within their respective domains
- **R-TDD-005** SHOULD: Test file paths SHOULD mirror production code paths with tests/Unit/Domains/ prefix replacing src/ or app/ prefix

### Scope

**In scope:**
- All unit test files under tests/Unit/Domains/
- Integration tests that follow domain-driven organization
- Feature module test suites within Settings and other domains
- CI/CD pipeline test execution strategies

**Out of scope:**
- End-to-end tests that span multiple domains
- Legacy test files not yet migrated to domain structure
- Third-party package tests
- Performance or load tests with different organizational needs

**Exceptions:**
- EXC-001: Shared test utilities or base test classes that serve multiple domains
- EXC-002: Migration period for legacy tests being refactored into domain structure

### Verify

```bash
# Check test files exist under domain structure
find tests/Unit/Domains -type f -name '*Test.php' | head -20

# Verify namespace conventions
grep -r 'namespace.*Tests\\Unit\\Domains' tests/Unit/Domains/ | head -10

# Verify domain feature modules are organized as subdirectories
ls -R tests/Unit/Domains/Settings/ | grep -E '(ManageTemplates|ManageUserPreferences|ManageUsers)'
```

**Accept when:**
- Test files exist under tests/Unit/Domains/ with subdirectories matching production domain structure
- Test file names consistently append 'Test' suffix to production class names
- Domain feature modules (ManageTemplates, ManageUserPreferences, ManageUsers) are represented as subdirectories within their respective domains
- Test file paths mirror production code paths with tests/Unit/Domains/ prefix replacing src/ or app/ prefix

### Enforcement

<enforcement>
Claude Code MUST verify test file organization when creating or modifying test files. Verification includes:
- Confirming test files are placed in correct domain directories
- Validating test file naming conventions with 'Test' suffix
- Ensuring test paths mirror production code structure
- Checking domain feature modules are properly organized as subdirectories

**Violation handling:**
- Flag test files in incorrect locations with suggested correct paths
- Require test reorganization before proceeding with implementation
- Document exceptions using EXC-ID references when approved

Claude Code MUST NOT skip or defer verification when working with test files.
</enforcement>

### Implementation Guidance

- Create tests/Unit/Domains/ directory structure mirroring app/Domains/ or src/Domains/
- Navigate to corresponding production code location and replicate path under tests/Unit/Domains/
- Configure CI/CD pipelines to support domain-based test execution using path filters (e.g., phpunit --filter tests/Unit/Domains/Settings/)
- Use concise yet descriptive naming conventions for domain and module directories to minimize path length
- Consider IDE templates or code generation tools for automatic test file creation in correct locations

### Rationale

- Detected pattern across 17 test files with 79.47% confidence indicates widespread adoption
- Enables targeted test execution, reducing CI/CD pipeline duration
- Improves discoverability through predictable hierarchical organization
- Supports parallel test execution and domain-specific ownership
- Facilitates microservices or modular monolith architectures