<rule_activation id="1b7c8081-8b34-44d0-ae87-2fc7d9db81fb" title="Adopt Laravel Migrations as Standard Database Schema Management: Migration Files Atomic" applies_to="**/*">
These rules are ALWAYS ACTIVE for all database schema changes across the application. All schema modifications MUST be implemented through Laravel migration files.
</rule_activation>

### Rules

- **R-DB-001** MUST: All schema modifications MUST be implemented through Laravel migration files in database/migrations directory.
- **R-DB-002** SHOULD: Migration files SHOULD be atomic, focusing on a single logical schema change to facilitate easier rollback and debugging.
- **R-DB-003** MUST: Each migration file MUST contain both up() and down() methods using Schema Builder API.
- **R-DB-004** MUST: Migration files MUST follow Laravel naming convention with timestamps (YYYY_MM_DD_HHMMSS_description.php).
- **R-DB-005** MUST: No direct SQL schema modifications are allowed in application code outside migration files (exceptions: EXC-001 emergency production hotfix, EXC-002 database-specific optimizations requiring raw SQL).
- **R-DB-006** SHOULD: Use Laravel migration methods like foreignId(), timestamps(), softDeletes() for consistency with framework conventions.
- **R-DB-007** MUST: Always test migrations locally with both 'migrate' and 'migrate:rollback' commands before committing.

### In Scope

- All table creation, modification, and deletion operations
- Column additions, modifications, and removals
- Index creation and removal
- Foreign key constraint definitions
- Database-level configuration changes that affect schema structure

### Out of Scope

- Application data seeding for testing purposes (use database/seeders instead)
- Data migration or transformation scripts that don't modify schema structure
- Database performance tuning that doesn't affect schema (query optimization, connection pooling)
- Backup and restore operations

### Verify

```bash
# Verify migration files exist and follow naming convention
find database/migrations -name '*.php' -type f | wc -l | grep -E '^[0-9]+$'

# Check for Schema Builder usage in migrations
grep -r 'Schema::create\|Schema::table\|Schema::drop' database/migrations/ | wc -l

# Verify migration status
php artisan migrate:status 2>&1 | grep -E 'Ran|Pending'

# Verify up() and down() methods exist in migration files
ls database/migrations/*.php 2>/dev/null | head -5 | xargs grep -l 'function up()' | xargs grep -l 'function down()'
```

**Accept when:**
- All migration files exist in database/migrations directory and follow Laravel naming convention with timestamps
- Each migration file contains both up() and down() methods using Schema Builder API
- Migration status command executes successfully showing tracked migrations
- No direct SQL schema modifications are found in application code outside migration files

<enforcement>
Claude Code MUST NOT skip or defer verification. All schema changes MUST be reviewed to ensure compliance with migration-based approach. Pull requests containing direct SQL schema changes outside migration files must be flagged and rejected.
</enforcement>