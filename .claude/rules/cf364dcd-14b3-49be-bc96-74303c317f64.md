<rule_activation id="cf364dcd-14b3-49be-bc96-74303c317f64" title="Standardize Public API Contract Design with Interface-Based DTOs and Exception Handling: Public Endpoints External" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching public API endpoints, external integration points, and API contract boundaries.
</rule_activation>

### Rules

- **R-API-001** MUST: All public API endpoints and external integration points MUST define explicit interface contracts (e.g., IDAVBackend) that declare method signatures, parameters, and return types.
- **R-API-002** MUST: All API request/response payloads MUST use dedicated DTO classes (suffixed with 'Dto') rather than exposing domain models directly.
- **R-API-003** MUST: All public API error conditions MUST throw domain-specific exceptions (suffixed with 'Exception') with clear, semantic error messages.
- **R-API-004** MUST: All interface methods MUST include PHP type hints and return type declarations to enforce contract compliance at runtime.
- **R-API-005** MUST: All public interfaces and DTOs MUST be documented with PHPDoc annotations including @api tag to clearly mark public contracts.
- **R-API-006** SHOULD: Implement centralized exception handler that maps domain exceptions to appropriate HTTP status codes and error response formats.
- **R-API-007** SHOULD: Use naming conventions: interfaces prefixed with 'I' (IDAVBackend), DTOs suffixed with 'Dto', exceptions suffixed with 'Exception'.
- **R-API-008** SHOULD: Consider implementing OpenAPI/Swagger specifications generated from interface definitions for external documentation.
- **R-API-009** MAY: Create base DTO abstract class with common serialization/validation logic to reduce boilerplate in concrete DTOs.

### Verify

```bash
# Count DTO classes in domains
grep -r "class.*Dto" app/Domains --include="*.php" | wc -l

# Count interface definitions
grep -r "interface I[A-Z]" app/Domains --include="*.php" | wc -l

# Count custom exception classes
grep -r "class.*Exception extends" app/Exceptions --include="*.php" | wc -l

# Count ViewHelper classes
find app/Domains -name "*ViewHelper.php" -type f | wc -l
```

**Accept when:**
- All public API endpoints have corresponding interface definitions with explicit method signatures
- All API request/response payloads use dedicated DTO classes rather than exposing domain models
- All public API error conditions throw domain-specific exceptions with clear messages
- Grep commands show consistent presence of DTOs, interfaces, custom exceptions, and ViewHelpers across API-related code
- All interface methods include PHP type hints and return type declarations
- All public interfaces and DTOs are documented with PHPDoc @api annotations

<enforcement>
Claude Code MUST NOT skip or defer verification. All rules in this activation are mandatory for public API endpoints and external integration points. Violations must be caught during code review and CI pipeline validation.
</enforcement>