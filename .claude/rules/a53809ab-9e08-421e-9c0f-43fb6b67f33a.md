<rule_activation id="a53809ab-9e08-421e-9c0f-43fb6b67f33a" title="Standardize Eloquent Models as Primary Data Access Layer with Public API Contracts: Models Define Custom" applies_to="**/*.php">
These rules are ALWAYS ACTIVE for all data modeling and database access patterns within the application codebase.
</rule_activation>

### Rules

- **R-ELQ-001** MAY: Models MAY define custom query scopes to encapsulate common query patterns.

### Verify

```bash
# Check that all model classes extend Eloquent Model
grep -r "class.*extends.*Model" app/Models/ | wc -l

# Verify no syntax errors in model files
find app/Models -name '*.php' -exec php -l {} \; | grep -v 'No syntax errors'

# Count raw SQL queries outside models (should be minimal)
grep -r "DB::raw\|DB::select\|DB::statement" app/ --exclude-dir=Models | wc -l
```

**Accept when:**
- All model classes in app/Models extend Illuminate\Database\Eloquent\Model
- No syntax errors in model files and all models are loadable by PHP parser
- Raw database queries outside models are documented with justification or count is below threshold (e.g., <5 instances)

<enforcement>
Claude Code MUST verify Eloquent model compliance through automated static analysis. Code review MUST require justification for raw SQL queries outside models. Performance regression tests MUST fail if query counts exceed established thresholds.
</enforcement>