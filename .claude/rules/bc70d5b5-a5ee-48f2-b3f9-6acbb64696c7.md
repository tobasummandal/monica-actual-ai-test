<rule_activation id="bc70d5b5-a5ee-48f2-b3f9-6acbb64696c7" title="Adopt Internationalization (i18n) Language Files for Multi-Language Support: User Facing Strings" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching `**/*`.
</rule_activation>

### Rules

- **R-I18N-001** MUST: All user-facing strings MUST be externalized into language-specific resource files organized under a lang/ directory structure.

### Verify

```bash
# Verify language directory structure exists
find lang/ -type f -name '*.php' | wc -l | grep -E '[0-9]+'

# Check for hardcoded user-facing strings in application code
grep -r "echo\|print" app/ | grep -v "trans(\|__('\|@lang" || echo 'No hardcoded strings found'

# Run translation checks
php artisan lang:check --missing || composer run check-translations
```

**Accept when:**
- Language directory structure exists with at least one locale containing all required translation categories (auth, validation, passwords, actions, pagination, http-statuses, format)
- No hardcoded user-facing strings are found in application code outside of language files
- Translation loading mechanism is implemented and functional with fallback to default locale for missing keys

<enforcement>
Claude Code MUST NOT skip or defer verification. All user-facing strings must be validated against the lang/ directory structure and no hardcoded strings are permitted in application code.
</enforcement>