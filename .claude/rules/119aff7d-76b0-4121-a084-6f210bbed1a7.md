<rule_activation id="119aff7d-76b0-4121-a084-6f210bbed1a7" title="Adopt Domain-Driven Service Layer Architecture for Business Operations: Business Operations Implemented" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files in the codebase to ensure domain-driven service layer architecture for business operations.
</rule_activation>

### Rules

- **R-DDS-001** MUST: Business operations MUST be implemented as dedicated service classes within domain-specific namespaces (e.g., app/Domains/{Domain}/{Feature}/Services/)
- **R-DDS-002** MUST: Service classes MUST be placed in Services/ subdirectories within domain/feature folders
- **R-DDS-003** MUST: Controllers MUST be placed in Web/Controllers/ subdirectories within domain/feature folders
- **R-DDS-004** MUST: Controllers MUST remain thin, limiting them to request validation, service invocation, and response formatting
- **R-DDS-005** SHOULD: Service classes SHOULD be named after the business operation they perform (e.g., CreateAccount, AcceptInvitation)
- **R-DDS-006** SHOULD: Use dependency injection to provide services to controllers, avoiding direct instantiation
- **R-DDS-007** SHOULD: Create domain folders under app/Domains/ with subfolders for each feature area (e.g., CreateAccount, CancelAccount)

### Scope

**In scope:**
- All business logic for account management operations
- User invitation and acceptance workflows
- Domain-specific service classes handling core business rules
- Web controllers that serve as entry points for HTTP requests

**Out of scope:**
- Infrastructure-level services (database, caching, queuing)
- Third-party API integrations that don't represent core business logic
- Utility classes and helpers that provide cross-cutting concerns
- Framework-level middleware and request handling

### Verify

```bash
# Check for service classes in domain structure
find app/Domains -type f -name '*.php' -path '*/Services/*' | wc -l

# Verify service namespaces follow pattern
grep -r 'namespace.*Domains.*Services' app/Domains --include='*.php' | wc -l

# Check for controllers in domain structure
find app/Domains -type f -name '*Controller.php' -path '*/Web/Controllers/*' | wc -l
```

**Accept when:**
- Service classes exist in app/Domains/{Domain}/{Feature}/Services/ directories
- Controllers exist in app/Domains/{Domain}/{Feature}/Web/Controllers/ directories
- At least 3 domain service classes follow the established pattern (matching detected evidence)
- Controllers delegate business logic to service classes rather than implementing it directly

<enforcement>
Claude Code MUST NOT skip or defer verification. Before completing any task involving business operations or domain services, verify compliance using the provided bash commands and accept criteria.

Violation handling:
- CI build fails if controllers contain business logic exceeding complexity thresholds
- Pull requests are blocked if service classes are not placed in correct domain directories
- Architecture review required for new domains or significant deviations from pattern

Exception process:
- Document exception rationale in ADR amendment or inline code comments
- Obtain approval from tech lead or architecture review board
- Add technical debt ticket if exception is temporary workaround
</enforcement>

### Rationale

This pattern was detected with 79.20% confidence across the codebase. Key benefits:
- Improved testability through isolation of business logic from web framework concerns
- Enhanced maintainability with clear separation of concerns and domain boundaries
- Better code reusability as services can be invoked from multiple controllers or other services
- Easier onboarding for new developers who can navigate the codebase by domain and feature

### References

- ADR Status: proposed
- ADR Date: 2024-01-15
- Deciders: Detection Pipeline (automated)