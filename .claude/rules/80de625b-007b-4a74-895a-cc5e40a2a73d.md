<rule_activation id="80de625b-007b-4a74-895a-cc5e40a2a73d" title="Standardize Unit and Feature Test Organization in CI/CD Pipeline: Pipelines Run Unit" applies_to="**/*">
These rules are ALWAYS ACTIVE for all PHP test files in tests/Unit and tests/Feature directories, CI/CD pipeline configuration files, and test execution automation.
</rule_activation>

### Rules

- **R-TEST-001** MAY: CI/CD pipelines MAY run unit and feature tests in parallel to optimize build time.

### Verify

```bash
# Count unit tests
grep -r "class.*Test extends" tests/Unit/ | wc -l

# Count feature tests
grep -r "class.*Test extends" tests/Feature/ | wc -l

# Run unit tests with fail-fast
php artisan test --testsuite=unit --stop-on-failure

# Run feature tests with fail-fast
php artisan test --testsuite=feature --stop-on-failure
```

**Accept when:**
- Both unit and feature test directories exist and contain test files following the *Test.php naming convention
- PHPUnit configuration defines separate test suites for unit and feature tests
- CI/CD pipeline successfully executes both test suites sequentially (unit first, then feature) on every commit
- All tests pass in both suites without errors or warnings

<enforcement>
Claude Code MUST verify test organization compliance using the provided bash commands before accepting changes to test files or CI/CD pipeline configuration. Verification is mandatory and MUST NOT be skipped or deferred.
</enforcement>