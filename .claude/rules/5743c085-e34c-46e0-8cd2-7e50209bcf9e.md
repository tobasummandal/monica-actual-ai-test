<rule_activation id="5743c085-e34c-46e0-8cd2-7e50209bcf9e" title="Organize Domain Logic Using Domain-Driven Design Bounded Contexts: Controllers Delegate Business" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching the domain structure pattern, particularly within app/Domains bounded contexts.
</rule_activation>

### Rules

- **R-DDD-001** SHOULD: Controllers SHOULD delegate business logic to service classes rather than implementing logic directly.

### Verify

```bash
# Verify all controllers are in Web/Controllers subdirectories
find app/Domains -type f -name '*Controller.php' | xargs grep -L 'namespace.*\\Web\\Controllers' | wc -l | grep -q '^0$'

# Count service classes in Services subdirectories
find app/Domains -type f -name '*Service.php' -path '*/Services/*' | wc -l

# Verify no controllers exist outside Web/Controllers namespaces
grep -r 'class.*Controller' app/Domains --include='*.php' | grep -v 'namespace.*\\Web\\Controllers' | wc -l | grep -q '^0$'
```

**Accept when:**
- All controllers are located in Web/Controllers subdirectories within their respective domains
- Service classes are located in Services subdirectories and contain business logic separate from HTTP concerns
- New features added to the codebase follow the domain structure pattern with at least 95% compliance
- Code review checklist includes verification of proper domain organization
- Controllers are thin adapters that validate input, call appropriate services, and format responses
- Service classes follow a command pattern with a single public execute() or handle() method
- Dependencies are provided via dependency injection rather than direct instantiation
- Domain events are used for cross-bounded-context communication

<enforcement>
Claude Code MUST verify domain organization compliance before approving code changes. Violations must be resolved via refactoring or documented exceptions before merge. Automated static analysis and CI pipeline checks are mandatory.
</enforcement>