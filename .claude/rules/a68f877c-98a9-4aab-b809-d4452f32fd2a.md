<rule_activation id="a68f877c-98a9-4aab-b809-d4452f32fd2a" title="Standardize Unit and Feature Test Organization in CI/CD Pipeline: Unit Tests Placed" applies_to="tests/**/*.php">
These rules are ALWAYS ACTIVE for all PHP test files in the tests/ directory.
</rule_activation>

### Rules

- **R-TEST-001** MUST: All unit tests MUST be placed in the tests/Unit directory and organized by domain boundaries

### Verify

```bash
# Count unit tests
grep -r "class.*Test extends" tests/Unit/ | wc -l

# Count feature tests
grep -r "class.*Test extends" tests/Feature/ | wc -l

# Run unit tests with fail-fast
php artisan test --testsuite=unit --stop-on-failure

# Run feature tests with fail-fast
php artisan test --testsuite=feature --stop-on-failure
```

**Accept when:**
- Both unit and feature test directories exist and contain test files following the *Test.php naming convention
- PHPUnit configuration defines separate test suites for unit and feature tests
- CI/CD pipeline successfully executes both test suites sequentially (unit first, then feature) on every commit
- All tests pass in both suites without errors or warnings
- Test files are organized by domain boundaries (Contact, DAV, Jetstream, etc.)

<enforcement>
Claude Code MUST NOT skip or defer verification. All test files MUST comply with R-TEST-001 placement requirements. CI/CD pipeline MUST fail if tests are placed in incorrect directories or don't follow naming conventions.
</enforcement>