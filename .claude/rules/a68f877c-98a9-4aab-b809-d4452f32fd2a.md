<rule_activation id="a68f877c-98a9-4aab-b809-d4452f32fd2a" title="Standardize Unit and Feature Test Organization in CI/CD Pipeline: Unit Tests Placed" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files in the project to ensure proper test organization and CI/CD execution.
</rule_activation>

### Rules

- **R-UNIT-001** MUST: All unit tests MUST be placed in the tests/Unit directory and organized by domain boundaries.

### Verify

```bash
# Count unit test classes
grep -r "class.*Test extends" tests/Unit/ | wc -l

# Count feature test classes
grep -r "class.*Test extends" tests/Feature/ | wc -l

# Run unit test suite
php artisan test --testsuite=unit --stop-on-failure

# Run feature test suite
php artisan test --testsuite=feature --stop-on-failure
```

**Accept when:**
- Both unit and feature test directories exist and contain test files following the *Test.php naming convention
- PHPUnit configuration defines separate test suites for unit and feature tests
- CI/CD pipeline successfully executes both test suites sequentially (unit first, then feature) on every commit
- All tests pass in both suites without errors or warnings

<enforcement>
Claude Code MUST verify test organization before committing changes. Tests placed in incorrect directories must be moved to comply with this standard. Verification cannot be skipped or deferred.
</enforcement>