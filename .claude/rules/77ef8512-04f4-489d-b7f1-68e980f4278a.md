<rule_activation id="77ef8512-04f4-489d-b7f1-68e980f4278a" title="Adopt PHP as Primary Backend Language for Laravel-Based Web Applications: Authentication Security Critical" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files in the Laravel-based web application codebase.
</rule_activation>

### Rules

- **R-PHP-001** MUST: Authentication and security-critical operations (password reset, user management) MUST execute within the PHP runtime using Laravel's built-in security features.
- **R-PHP-002** MUST: All backend business logic and domain services MUST be implemented in PHP.
- **R-PHP-003** MUST: Authentication and authorization mechanisms (Fortify actions) MUST be implemented in PHP.
- **R-PHP-004** MUST: Web controllers and HTTP request handling MUST be implemented in PHP.
- **R-PHP-005** MUST: Database migrations and ORM operations MUST be implemented in PHP.
- **R-PHP-006** MUST: Feature and unit tests for backend functionality MUST be written in PHP.
- **R-PHP-007** MUST: API endpoints and web routes MUST be implemented in PHP.
- **R-PHP-008** MUST: Specify minimum PHP version 8.0+ in composer.json platform requirements.
- **R-PHP-009** SHOULD: Use Laravel Sail or Docker Compose for local development to ensure environment parity with production.
- **R-PHP-010** SHOULD: Document required PHP extensions and configuration in deployment runbooks.
- **R-PHP-011** SHOULD: Implement automated testing in CI/CD against the target PHP version.
- **R-PHP-012** SHOULD: Use PHP-FPM with nginx or Apache for production deployments.

### Verify

```bash
# Verify PHP version compatibility
php --version | grep -E 'PHP [8-9]\.' && echo 'PHP version compatible' || echo 'PHP version incompatible'

# Verify required Laravel PHP extensions are present
php -m | grep -E '(openssl|pdo|mbstring|tokenizer|xml|ctype|json|bcmath)' | wc -l | grep -E '[8-9]|[1-9][0-9]' && echo 'Required extensions present' || echo 'Missing extensions'

# Verify PHP files detected in app directory
find app/ -name '*.php' -type f | wc -l | grep -E '[1-9][0-9]*' && echo 'PHP files detected in app directory' || echo 'No PHP files found'
```

**Accept when:**
- PHP version check returns 8.0 or higher in all deployment environments
- All required Laravel PHP extensions (OpenSSL, PDO, Mbstring, Tokenizer, XML, Ctype, JSON, BCMath) are installed and enabled
- Application backend code in app/ directory consists primarily of PHP files with .php extension
- Composer dependencies successfully install and application bootstraps without runtime errors

<enforcement>
Verification via CI/CD pipeline checks for PHP version compatibility and extension availability is MANDATORY. Code review process MUST ensure new backend code is written in PHP following Laravel conventions. Deployment validation scripts MUST verify runtime environment meets PHP requirements before release. Claude Code MUST NOT skip or defer verification of these rules.
</enforcement>