<rule_activation id="ad332797-5cfe-4ff4-a90e-cb3b791d9116" title="Adopt Modular Architecture with Factory-Based Component Generation: Database Entity Factories" applies_to="**/*.php">
These rules are ALWAYS ACTIVE for all PHP files matching `**/*.php`.
</rule_activation>

### Rules

- **R-MOD-001** MUST: Database entity factories MUST extend the framework's base Factory class and be placed in database/factories/ directory
- **R-MOD-002** MUST: All PHP files in app/Domains directory MUST use the correct App\Domains namespace hierarchy
- **R-MOD-003** MUST: All setup commands in app/Console/Commands MUST extend the framework's Command class and follow naming conventions

### Context

**In scope:**
- All application modules and domain components
- Database factories for test data generation and seeding
- Console commands for setup, initialization, and maintenance
- View helpers and presentation layer components
- Scheduled task definitions and cron job management

**Out of scope:**
- Third-party vendor packages and external libraries
- Framework core files and base classes
- Configuration files and environment-specific settings
- Static assets and frontend build artifacts

**Exceptions:**
- EX-001: Legacy modules predating this ADR that require significant refactoring effort
- EX-002: Rapid prototyping or proof-of-concept code in designated experimental branches

### Verify

```bash
# Verify all PHP files in app/Domains use correct namespace
find app/Domains -type f -name '*.php' | xargs grep -L 'namespace App\\Domains' | wc -l | grep -q '^0$'

# Verify all factory classes extend base Factory
find database/factories -type f -name '*Factory.php' | xargs grep -c 'extends Factory' | grep -v ':0'

# Verify all setup commands extend Command class
find app/Console/Commands -type f -name 'Setup*.php' | xargs grep -c 'extends Command' | grep -v ':0'
```

**Accept when:**
- All PHP files in app/Domains directory use the correct App\Domains namespace hierarchy
- All factory classes in database/factories extend the framework's base Factory class
- All setup commands in app/Console/Commands extend the framework's Command class and follow naming conventions

<enforcement>
Claude Code MUST NOT skip or defer verification. Violations result in:
- CI pipeline failure blocking merge
- Code review approval withheld until compliance achieved
- Technical debt tickets created for legacy violations with prioritized remediation plan

Exception process requires:
1. Developer submits exception request to Technical Lead with justification
2. Technical Lead reviews with Architecture Review Board if affecting multiple modules
3. Approved exceptions documented in ADR exceptions log with expiration date and migration plan
</enforcement>