<rule_activation id="ad332797-5cfe-4ff4-a90e-cb3b791d9116" title="Adopt Modular Architecture with Factory-Based Component Generation: Database Entity Factories" applies_to="**/*.php">
These rules are ALWAYS ACTIVE for all PHP files in the application. They enforce modular architecture with factory-based component generation across database entities, console commands, and domain modules.
</rule_activation>

### Rules

- **R-MOD-001** MUST: Database entity factories MUST extend the framework's base Factory class and be placed in database/factories/ directory
- **R-MOD-002** MUST: All PHP files in app/Domains directory MUST use the correct App\Domains namespace hierarchy
- **R-MOD-003** MUST: All factory classes in database/factories MUST extend the framework's base Factory class
- **R-MOD-004** MUST: All setup commands in app/Console/Commands MUST extend the framework's Command class and follow naming conventions
- **R-MOD-005** SHOULD: Domain names should be singular nouns (Contact, not Contacts)
- **R-MOD-006** SHOULD: Subdomain names should describe the capability (ManageContact)
- **R-MOD-007** SHOULD: Layer names should be standard (Web, Console, Database)
- **R-MOD-008** SHOULD: Factory updates should be included in definition of done for model changes
- **R-MOD-009** MAY: Simplified approach may be used for simple CRUD modules with documented justification

### Verify

```bash
# Verify all PHP files in app/Domains use correct namespace
find app/Domains -type f -name '*.php' | xargs grep -L 'namespace App\\Domains' | wc -l | grep -q '^0$'

# Verify all factory classes extend base Factory class
find database/factories -type f -name '*Factory.php' | xargs grep -c 'extends Factory' | grep -v ':0'

# Verify all setup commands extend Command class
find app/Console/Commands -type f -name 'Setup*.php' | xargs grep -c 'extends Command' | grep -v ':0'
```

**Accept when:**
- All PHP files in app/Domains directory use the correct App\Domains namespace hierarchy
- All factory classes in database/factories extend the framework's base Factory class
- All setup commands in app/Console/Commands extend the framework's Command class and follow naming conventions
- Factory-generated data passes model validation in CI checks
- Module dependencies are documented and no circular dependencies exist
- Code review checklist confirms correct namespace, factory presence, setup commands, and view helpers placement

<enforcement>
Claude Code MUST NOT skip or defer verification. All R-MOD-### rules marked MUST are mandatory and must be verified before accepting code changes. Violations block merge and require remediation or approved exception with Technical Lead review.
</enforcement>