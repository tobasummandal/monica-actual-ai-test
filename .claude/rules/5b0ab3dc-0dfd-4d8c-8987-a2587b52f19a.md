<rule_activation id="5b0ab3dc-0dfd-4d8c-8987-a2587b52f19a" title="Adopt Eloquent ORM Models as Standard Data Modeling Pattern: Each Model Class" applies_to="app/Models/**/*.php">
These rules are ALWAYS ACTIVE for all data modeling implementations in the application layer. All domain entities requiring database persistence MUST be represented using Eloquent ORM models.
</rule_activation>

### Rules

- **R-ELQ-001** MUST: Each model class MUST correspond to a single database table with clear naming conventions (singular model name, plural table name by default).
- **R-ELQ-002** MUST: All model files in app/Models directory extend Illuminate\Database\Eloquent\Model.
- **R-ELQ-003** MUST: Define $fillable or $guarded properties in each model to control mass assignment. Prefer $fillable for explicit whitelisting.
- **R-ELQ-004** MUST: Use $casts property to define attribute type casting (e.g., 'is_active' => 'boolean', 'metadata' => 'array', 'published_at' => 'datetime').
- **R-ELQ-005** MUST: Define relationships using Eloquent methods (hasMany, belongsTo, belongsToMany, morphMany, etc.) with proper return type hints.
- **R-ELQ-006** SHOULD: Use query scopes (scopeActive, scopeRecent, etc.) for reusable query logic specific to the model.
- **R-ELQ-007** SHOULD: Leverage model events (creating, created, updating, updated, deleting, deleted) for cross-cutting concerns like logging or cache invalidation.
- **R-ELQ-008** SHOULD: Use eager loading (with() method) to prevent N+1 query problems when accessing relationships.
- **R-ELQ-009** SHOULD: Consider using API resources or transformers for serialization rather than exposing raw model attributes.
- **R-ELQ-010** MUST NOT: Place excessive business logic in model classes; business logic belongs in service classes or domain services.

### Verify

```bash
# Count Eloquent models in app/Models directory
grep -r "extends Model" app/Models/ | wc -l

# Find model files missing Eloquent import
find app/Models -type f -name '*.php' -exec grep -L 'use Illuminate\\Database\\Eloquent\\Model' {} \;

# Display model structure summary
php artisan model:show --all 2>&1 | grep -E '(class|table)' | head -20

# Verify fillable/guarded properties exist
grep -r "\$fillable\|\$guarded" app/Models/ | wc -l

# Check for $casts property usage
grep -r "\$casts" app/Models/ | wc -l
```

**Accept when:**
- All model files in app/Models directory extend Illuminate\Database\Eloquent\Model
- Each model class corresponds to a database table and follows Laravel naming conventions (singular PascalCase model name)
- Models define appropriate fillable/guarded properties for mass assignment protection
- Models define $casts property for attribute type safety
- Relationships between entities are defined using Eloquent relationship methods with proper return type hints
- Query scopes are used for reusable model-specific query logic
- Eager loading (with() method) is used to prevent N+1 query problems
- No raw SQL queries exist where Eloquent could reasonably be used instead
- Business logic is not placed directly in model classes

<enforcement>
Claude Code MUST verify all R-ELQ rules before accepting model class implementations. Verification is mandatory via automated checks in CI pipeline and manual code review. Models created outside app/Models directory or not extending Eloquent Model base class MUST be rejected. Static analysis warnings for missing fillable/guarded properties or $casts MUST be resolved before merge.
</enforcement>