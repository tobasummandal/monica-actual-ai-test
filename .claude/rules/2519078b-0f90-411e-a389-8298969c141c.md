<rule_activation id="2519078b-0f90-411e-a389-8298969c141c" title="Adopt Inertia.js for Server-Driven Single Page Application Rendering: Form Submissions Return" applies_to="**/*Controller.php">
These rules are ACTIVE for all web controllers in the Vault/ManageJournals domain and SHOULD be applied to new controller implementations serving user-facing web interfaces.
</rule_activation>

### Rules

- **R-INERTIA-001** SHOULD: Form submissions SHOULD return Inertia responses with appropriate redirect() or back() methods to maintain SPA navigation.

### Verify

```bash
# Check that controllers use Inertia::render() for view responses
grep -r "Inertia::render" app/Domains/Vault/ManageJournals/Web/Controllers/ | wc -l

# Verify no traditional view() calls exist in ManageJournals controllers
grep -r "return view(" app/Domains/Vault/ManageJournals/Web/Controllers/ | wc -l

# Verify journal-related routes are handled by controllers
php artisan route:list --path=journals --json | jq '[.[] | select(.action | contains("Controller"))] | length'
```

**Accept when:**
- All controllers in ManageJournals domain use Inertia::render() for view responses (verify command 1 shows expected count)
- No traditional view() calls exist in ManageJournals controllers (verify command 2 returns 0)
- All journal-related routes are handled by controllers using Inertia responses
- Code review confirms props are properly structured and lazy loading is used for expensive data

<enforcement>
Claude Code MUST verify Inertia.js usage patterns in form submissions and controller responses. Verification MUST NOT be skipped or deferred.
</enforcement>