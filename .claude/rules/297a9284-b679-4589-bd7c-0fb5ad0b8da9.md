<rule_activation id="297a9284-b679-4589-bd7c-0fb5ad0b8da9" title="Adopt Domain-Driven Service Layer Pattern for Journal Management: Business Logic Journal" applies_to="app/Domains/Vault/ManageJournals/**/*">
These rules are ALWAYS ACTIVE for all files within the Vault/ManageJournals domain, ensuring business logic is properly encapsulated in service classes following domain-driven design principles.
</rule_activation>

### Rules

- **R-SVC-001** MUST: All business logic for journal management operations MUST be encapsulated in dedicated service classes within the app/Domains/Vault/ManageJournals/Services directory.

- **R-SVC-002** SHOULD: Use verb-noun naming for service classes (e.g., CreatePost, UpdateMetric, RemoveTag) to clearly communicate intent.

- **R-SVC-003** MUST: Controllers MUST remain thin - they should validate input, call services, and return responses without implementing business logic.

- **R-SVC-004** SHOULD: Services should accept primitive types or DTOs as parameters rather than HTTP request objects to maintain independence from web layer.

- **R-SVC-005** SHOULD: Consider using dependency injection for service dependencies to improve testability and enable mocking.

- **R-SVC-006** MAY: Group related services in subdirectories if a domain grows large (e.g., ManageJournals/Services/Posts, ManageJournals/Services/Metrics).

### Scope

**In scope:**
- All journal management operations including posts, metrics, photos, tags, slices of life, and contacts
- Business logic that requires database transactions or multiple model interactions
- Operations that enforce domain-specific business rules and validation
- Functionality within the Vault domain's ManageJournals subdomain

**Out of scope:**
- Simple CRUD operations that only involve single-model persistence without business logic
- HTTP request/response handling and view rendering (belongs in controllers)
- Cross-domain operations that span multiple bounded contexts
- Infrastructure concerns like caching, logging, and external API integration

### Verify

```bash
# Count service classes in Services directories
find app/Domains/*/Services -type f -name '*.php' | wc -l

# Count controllers in ManageJournals
grep -r 'class.*Controller' app/Domains/Vault/ManageJournals/Web/Controllers | wc -l

# Verify Services namespace usage
grep -r 'namespace.*Services' app/Domains/Vault/ManageJournals/Services | head -5
```

**Accept when:**
- Service classes exist in dedicated Services directories within domain boundaries
- Controllers delegate to services rather than implementing business logic directly
- Service class names follow verb-noun convention and implement single-responsibility operations
- Services maintain independence from HTTP layer by accepting primitives or DTOs

<enforcement>
Claude Code MUST NOT skip or defer verification. All journal management business logic changes require:
- Code review checking for business logic in controllers
- Verification that services follow single-responsibility principle
- Confirmation that controller methods remain thin and delegate to services
- Architecture review for significant deviations requiring documented exceptions
</enforcement>