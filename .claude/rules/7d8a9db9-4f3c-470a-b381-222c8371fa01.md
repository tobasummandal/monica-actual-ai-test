<rule_activation id="7d8a9db9-4f3c-470a-b381-222c8371fa01" title="Adopt Domain-Driven Controller Organization for Journal Management: Controllers Placed Within" applies_to="app/Domains/Vault/ManageJournals/**/*Controller.php">
These rules are ALWAYS ACTIVE for all Laravel controllers within the ManageJournals bounded context.
</rule_activation>

### Rules

- **R-DDC-001** MUST: Controllers MUST be placed within the Web/Controllers subdirectory of their domain to separate web concerns from other layers.

### Verify

```bash
# Verify all controller files exist in correct location
find app/Domains/Vault/ManageJournals/Web/Controllers -name '*Controller.php' | wc -l

# Verify all controllers use correct namespace
grep -r 'namespace App\\Domains\\Vault\\ManageJournals\\Web\\Controllers' app/Domains/Vault/ManageJournals/Web/Controllers/*.php | wc -l

# Verify routes reference correct controller namespace
php artisan route:list --path=journal --columns=Action | grep 'App\\Domains\\Vault\\ManageJournals\\Web\\Controllers'
```

**Accept when:**
- All controller files in the ManageJournals domain are located under `app/Domains/Vault/ManageJournals/Web/Controllers`
- Each controller has a single, clear responsibility corresponding to an entity or entity-concern combination
- Controller namespaces match their directory structure: `App\Domains\Vault\ManageJournals\Web\Controllers`
- Route definitions correctly reference controllers using the domain-based namespace
- Controllers handle journal, post, photo, metric, and slice of life entities

<enforcement>
Claude Code MUST NOT skip or defer verification. All controllers must be verified to exist in the correct domain-driven location with proper namespace declarations before accepting changes.
</enforcement>