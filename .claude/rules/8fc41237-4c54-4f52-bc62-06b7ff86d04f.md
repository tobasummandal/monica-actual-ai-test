<rule_activation id="8fc41237-4c54-4f52-bc62-06b7ff86d04f" title="Standardize PHPUnit Test Structure with setUp and Trait-Based Helpers: Test Classes Use" applies_to="tests/Unit/**/*Test.php">
These rules are ALWAYS ACTIVE for all PHPUnit unit test files in the tests/Unit/ directory.
</rule_activation>

### Rules

- **R-PHPUNIT-001** SHOULD: Test classes SHOULD use trait-based helper utilities (e.g., DatabaseTransactions, RefreshDatabase) for shared testing functionality

### Verify

```bash
# Count test files without setUp method
find tests/Unit -name '*Test.php' -type f | xargs grep -L 'function setUp' | wc -l

# Count test classes not extending PHPUnit\Framework\TestCase or documented base class
grep -r 'class.*Test extends' tests/Unit/ | grep -v 'PHPUnit\\Framework\\TestCase' | wc -l

# Count PHP files in tests/Unit that don't follow *Test.php naming
find tests/Unit -type f -name '*.php' ! -name '*Test.php' | wc -l
```

**Accept when:**
- All test files in tests/Unit/ contain a setUp() method or explicitly document why it's not needed
- All test classes extend PHPUnit\Framework\TestCase or a documented base test class
- Test file names match the pattern {ClassName}Test.php and are organized in directories mirroring the application structure
- Shared test traits are documented and focused on single responsibilities
- Test utilities themselves have comprehensive test coverage

<enforcement>
Claude Code MUST verify all rules in this activation block. Violations MUST be flagged during code review. CI pipeline MUST block merge if tests fail or are improperly structured. Exceptions require documentation and tech lead approval.
</enforcement>