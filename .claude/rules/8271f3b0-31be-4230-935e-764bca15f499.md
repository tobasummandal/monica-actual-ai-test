<rule_activation id="8271f3b0-31be-4230-935e-764bca15f499" title="Adopt Eloquent ORM Models as Standard Data Modeling Pattern: Model Classes Placed" applies_to="**/*">
These rules are ALWAYS ACTIVE for all data modeling activities within the application layer.
</rule_activation>

### Rules

- **R-29-001** MUST: Model classes MUST be placed in the app/Models namespace and directory structure.
- **R-29-008** MUST: Enforce single responsibility principle; extract business logic to service classes rather than bloating models.

### Verify

```bash
# Verify all PHP files in app/Models extend Eloquent Model
find app/Models -type f -name '*.php' | xargs grep -L 'extends Model' | wc -l | grep -q '^0$'

# Verify proper namespace usage
grep -r 'namespace App\\Models' app/Models/*.php | wc -l

# Verify model class structure
php artisan tinker --execute="echo count(get_declared_classes())" 2>&1 | grep -q '[0-9]'
```

**Accept when:**
- All PHP files in app/Models directory extend Illuminate\Database\Eloquent\Model class
- Model classes are properly namespaced under App\Models and follow PSR-4 autoloading standards
- No direct database queries using DB facade exist in controller methods where Eloquent models are available
- Code review checklist includes verification of mass assignment protection and relationship eager loading
- Models contain only data access concerns; business logic is extracted to service classes
- All models define $fillable or $guarded arrays for mass assignment protection
- Relationships are defined in dedicated methods and documented for bidirectional navigation

<enforcement>
Claude Code MUST verify all rules in this activation block. Static analysis, code review integration, and CI pipeline checks are mandatory. Violations MUST be caught before merge. Exceptions require documented rationale and technical lead approval.
</enforcement>